<Window x:Class="YoloAnnotationEditor.BatchLabelEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch Label Editor"
        Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        MinHeight="600" MinWidth="900">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step indicator -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Border x:Name="Step1Indicator" Background="#2196F3" CornerRadius="12" Padding="8,4" Margin="0,0,5,0">
                <TextBlock Text="1. Select Images" Foreground="White" FontWeight="SemiBold"/>
            </Border>
            <TextBlock x:Name="Step2Arrow" Text="→" VerticalAlignment="Center" Margin="5,0" FontSize="16" Foreground="Gray"/>
            <Border x:Name="Step2Indicator" Background="LightGray" CornerRadius="12" Padding="8,4" Margin="0,0,5,0">
                <TextBlock x:Name="Step2Text" Text="2. Draw Region" Foreground="DarkGray" FontWeight="SemiBold"/>
            </Border>
            <TextBlock Text="→" VerticalAlignment="Center" Margin="5,0" FontSize="16" Foreground="Gray"/>
            <Border x:Name="Step3Indicator" Background="LightGray" CornerRadius="12" Padding="8,4" Margin="0,0,5,0">
                <TextBlock x:Name="Step3Text" Text="3. Configure" Foreground="DarkGray" FontWeight="SemiBold"/>
            </Border>
            <TextBlock Text="→" VerticalAlignment="Center" Margin="5,0" FontSize="16" Foreground="Gray"/>
            <Border x:Name="Step4Indicator" Background="LightGray" CornerRadius="12" Padding="8,4">
                <TextBlock x:Name="Step4Text" Text="4. Apply" Foreground="DarkGray" FontWeight="SemiBold"/>
            </Border>
        </StackPanel>

        <!-- Step content area -->
        <Grid Grid.Row="1">

            <!-- ═══ STEP 1: Select Images + Operation ═══ -->
            <Grid x:Name="Step1Panel">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Operation selector -->
                <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="1"
                        CornerRadius="4" Padding="12" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Select operation:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <RadioButton x:Name="RbReplace" Content=" Replace class of labels in region"
                                         IsChecked="True" GroupName="Operation" FontSize="13"
                                         Checked="Operation_Changed" Margin="0,0,20,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <RadioButton x:Name="RbAdd" Content=" Add label if none exists in region"
                                         GroupName="Operation" FontSize="13"
                                         Checked="Operation_Changed" Margin="0,0,20,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <RadioButton x:Name="RbDelete" Content=" Delete all labels in region"
                                         GroupName="Operation" FontSize="13"
                                         Checked="Operation_Changed"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="RbRedetect" Content=" Redetect using YOLO model"
                                         GroupName="Operation" FontSize="13"
                                         Checked="Operation_Changed"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtOperationHint" Text=""
                                   Foreground="Gray" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="1" Text="Select the images you want to apply the operation to:"
                           FontSize="14" Margin="0,0,0,10"/>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Image list with checkboxes -->
                    <ListView Grid.Column="0" x:Name="LvImages"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              BorderThickness="1" BorderBrush="#FFABADB3"
                              PreviewMouseLeftButtonDown="LvImages_PreviewMouseLeftButtonDown">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Image Source="{Binding Thumbnail}" Width="60" Height="45" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding AnnotationCount, StringFormat=' ({0} labels)'}"
                                               VerticalAlignment="Center" Foreground="Gray" Margin="4,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Selection controls -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0" Width="130">
                        <Button Content="Select All" Click="BtnSelectAll_Click" Padding="5,4" Margin="0,0,0,5"/>
                        <Button Content="Select None" Click="BtnSelectNone_Click" Padding="5,4" Margin="0,0,0,5"/>
                        <Button Content="Invert Selection" Click="BtnInvertSelection_Click" Padding="5,4" Margin="0,0,0,15"/>

                        <TextBlock Text="Filter:" Margin="0,0,0,3"/>
                        <TextBox x:Name="TxtImageFilter" TextChanged="TxtImageFilter_TextChanged" Margin="0,0,0,10"/>

                        <TextBlock Text="Tip: Hold Shift and click to select a range of images."
                                   TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,10"/>

                        <Border Background="#F0F0F0" CornerRadius="4" Padding="8">
                            <StackPanel>
                                <TextBlock Text="Selection Summary" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtSelectionCount" Text="0 images selected"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- ═══ STEP 2: Draw Bounding Box Region ═══ -->
            <Grid x:Name="Step2Panel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" x:Name="TxtStep2Header"
                           Text="Draw a bounding box on the reference image to define the region."
                           FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>

                <!-- Reference image selector -->
                <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Reference Image:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ListView Grid.Row="1" x:Name="LvReferenceImages" SelectionChanged="LvReferenceImages_SelectionChanged"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              BorderThickness="1" BorderBrush="#FFABADB3">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <Image Source="{Binding Thumbnail}" Width="40" Height="30" Stretch="Uniform" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding FileName}" VerticalAlignment="Center" FontSize="11"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

                <!-- Canvas for drawing bounding box -->
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="#FFABADB3" BorderThickness="1" Background="LightGray">
                    <Grid>
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Grid x:Name="ImageContainer"
                                  PreviewMouseDown="Canvas_MouseDown"
                                  PreviewMouseMove="Canvas_MouseMove"
                                  PreviewMouseUp="Canvas_MouseUp">
                                <Image x:Name="ReferenceImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Canvas x:Name="DrawingCanvas" HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Background="Transparent"/>
                            </Grid>
                        </ScrollViewer>

                        <!-- Region info overlay -->
                        <Border Background="#CC000000" CornerRadius="4" Padding="8"
                                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10">
                            <TextBlock x:Name="TxtRegionInfo" Text="Click and drag to draw a region"
                                       Foreground="White" FontSize="12"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- ═══ STEP 3: Configure (dynamic per operation) ═══ -->
            <Grid x:Name="Step3Panel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" x:Name="TxtStep3Header"
                           Text="Configure the operation:"
                           FontSize="14" Margin="0,0,0,15"/>

                <!-- Class picker (shown for Replace and Add) -->
                <Grid Grid.Row="1" x:Name="ClassPickerPanel">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Search class:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="1" x:Name="TxtClassFilter" TextChanged="TxtClassFilter_TextChanged"/>
                    </Grid>
                    <ListBox Grid.Row="1" x:Name="LbClasses" Height="200"
                             SelectionChanged="LbClasses_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <Rectangle Width="16" Height="16" Fill="{Binding Color}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontSize="14"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <!-- Add-mode overlap options (shown only for Add) -->
                <Border Grid.Row="2" x:Name="OverlapOptionsPanel"
                        Background="#E3F2FD" BorderBrush="#1565C0" BorderThickness="1"
                        CornerRadius="4" Padding="12" Margin="0,12,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="How to handle images that already have a nearby label:"
                                   FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <RadioButton x:Name="RbAlwaysAdd"
                                     Content="Always add the new label (don't delete anything)"
                                     GroupName="OverlapMode" FontSize="13" Margin="0,0,0,8"
                                     Checked="OverlapMode_Changed"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <RadioButton x:Name="RbSkipIfNearby"
                                         Content="Skip if an existing label is within "
                                         GroupName="OverlapMode" FontSize="13" IsChecked="True"
                                         VerticalAlignment="Center" Checked="OverlapMode_Changed"/>
                            <TextBox x:Name="TxtSkipDistance" Width="55" Text="50"
                                     VerticalAlignment="Center" Padding="3,2" Margin="0,0,0,0"/>
                            <TextBlock Text=" px of the new label" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <RadioButton x:Name="RbDeleteNearbyThenAdd"
                                         Content="Delete existing labels within "
                                         GroupName="OverlapMode" FontSize="13"
                                         VerticalAlignment="Center" Checked="OverlapMode_Changed"/>
                            <TextBox x:Name="TxtDeleteDistance" Width="55" Text="50"
                                     VerticalAlignment="Center" Padding="3,2" Margin="0,0,0,0"/>
                            <TextBlock Text=" px, then add" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>

                        <TextBlock x:Name="TxtProximityNote"
                                   Text="Pixel distance is measured using the reference image dimensions."
                                   FontSize="11" Foreground="Gray" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Delete-mode: no class needed -->
                <Border Grid.Row="2" x:Name="DeleteInfoPanel"
                        Background="#FCE4EC" BorderBrush="#C62828" BorderThickness="1"
                        CornerRadius="4" Padding="12" Margin="0,0,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="No further configuration required." FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtDeletePreview" TextWrapping="Wrap" FontSize="13"/>
                    </StackPanel>
                </Border>

                <!-- Redetect-mode config -->
                <Border Grid.Row="2" x:Name="RedetectConfigPanel"
                        Background="#E8EAF6" BorderBrush="#3F51B5" BorderThickness="1"
                        CornerRadius="4" Padding="12" Margin="0,0,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Choose which images to redetect:" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <RadioButton x:Name="RbRedetectAll"
                                     Content="Redetect all selected images (delete existing labels and replace with YOLO detections)"
                                     GroupName="RedetectMode" FontSize="13" IsChecked="True" Margin="0,0,0,10"/>

                        <RadioButton x:Name="RbRedetectUnlabeledOnly"
                                     Content="Only redetect images that have no existing labels (skip already-labeled images)"
                                     GroupName="RedetectMode" FontSize="13" Margin="0,0,0,10"/>

                        <Border Background="#C5CAE9" CornerRadius="4" Padding="10" Margin="0,5,0,0">
                            <StackPanel>
                                <TextBlock Text="Detection settings:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtConfidenceDisplay" Text="Confidence threshold: > 0.4" FontSize="12" Foreground="#37474F"/>
                                <TextBlock Text="Minimum bounding box size: 5 × 5 px" FontSize="12" Foreground="#37474F"/>
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="TxtRedetectSummary" TextWrapping="Wrap" FontSize="13"
                                   Foreground="#283593" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Preview summary -->
                <Border Grid.Row="3" Background="#FFF3E0" BorderBrush="#FFB74D" BorderThickness="1"
                        CornerRadius="4" Padding="12" Margin="0,15,0,0"
                        x:Name="PreviewBorder" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Preview" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtPreviewSummary" TextWrapping="Wrap" FontSize="13"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ═══ STEP 4: Apply with progress ═══ -->
            <Grid x:Name="Step4Panel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Confirmation message -->
                <Border Grid.Row="0" Background="#FFEBEE" BorderBrush="#E53935" BorderThickness="1"
                        CornerRadius="4" Padding="15" Margin="0,0,0,15"
                        x:Name="ConfirmationBorder">
                    <StackPanel>
                        <TextBlock x:Name="TxtConfirmationTitle" Text="⚠ Confirm Batch Operation"
                                   FontWeight="Bold" FontSize="16" Foreground="#C62828" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtConfirmationDetails" TextWrapping="Wrap" FontSize="13"/>
                        <TextBlock Text="This operation writes directly to label files and cannot be undone."
                                   FontStyle="Italic" Foreground="#C62828" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Progress -->
                <StackPanel Grid.Row="1" x:Name="ProgressPanel" Visibility="Collapsed">
                    <TextBlock x:Name="TxtProgressStatus" Text="Processing..." FontSize="14" Margin="0,0,0,5"/>
                    <ProgressBar x:Name="ProgressBar" Height="25" Minimum="0" Maximum="100" Value="0"/>
                    <TextBlock x:Name="TxtProgressPercent" Text="0%" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                </StackPanel>

                <!-- Results -->
                <Border Grid.Row="2" x:Name="ResultsBorder" Visibility="Collapsed"
                        Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1"
                        CornerRadius="4" Padding="15" Margin="0,15,0,0">
                    <StackPanel>
                        <TextBlock Text="Batch Operation Complete" FontWeight="Bold" FontSize="16"
                                   Foreground="#2E7D32" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtResults" TextWrapping="Wrap" FontSize="13"/>
                    </StackPanel>
                </Border>

                <!-- Log -->
                <TextBlock Grid.Row="3" Text="Log:" FontWeight="SemiBold" Margin="0,15,0,5"
                           x:Name="LogLabel" Visibility="Collapsed"/>
                <TextBox Grid.Row="4" x:Name="TxtLog" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                         FontFamily="Consolas" FontSize="11" Visibility="Collapsed"/>
            </Grid>
        </Grid>

        <!-- Status bar -->
        <TextBlock Grid.Row="2" x:Name="TxtStatus" Text="Step 1: Choose operation and select images"
                   Margin="0,10,0,5" Foreground="Gray"/>

        <!-- Navigation buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button x:Name="BtnBack" Content="← Back" Click="BtnBack_Click"
                    Width="100" Padding="5,6" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="BtnNext" Content="Next →" Click="BtnNext_Click"
                    Width="100" Padding="5,6" Margin="0,0,10,0"/>
            <Button x:Name="BtnApply" Content="Apply Changes" Click="BtnApply_Click"
                    Width="130" Padding="5,6" Visibility="Collapsed"
                    Background="#E53935" Foreground="White" FontWeight="SemiBold"/>
            <Button x:Name="BtnClose" Content="Close" Click="BtnClose_Click"
                    Width="80" Padding="5,6"/>
        </StackPanel>
    </Grid>
</Window>
